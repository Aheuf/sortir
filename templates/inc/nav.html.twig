<nav class="navbar navbar-expand-lg navbar-dark bg-dark justify-content-end">
    <a class="nav-link link-light" href="#">Accueil</a>

    <a class="nav-link link-light" href="{{ path('admin_villes') }}">Ville</a>
    <a class="nav-link link-light" href="{{ path('admin_campus') }}">Campus</a>

    <a class="nav-link link-light" href="{{ path('app_account_show',{id:app.user.id}) }}">Mon profil</a>
    <a class="nav-link link-light me-5" href="{{ path('app_logout') }}">Deconnexion</a>
</nav>